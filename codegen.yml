overwrite: true
schema: 'http://localhost:4000/graphql'
generates:
  api.graphql:
    plugins:
      - schema-ast
    config:
      includeDirectives: true
      commentDescriptions: true
  src/api/types.ts:
    plugins:
      - typescript
